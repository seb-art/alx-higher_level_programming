#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
    for(idx in range(len(my_list)):
            if(idx < 0):
                None
            elif(idx >= len(my_list):
                return None
             else:
                return my_list[idx]
